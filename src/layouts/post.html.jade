---
layout: default
---
!=partial('blog-nav.html.jade')

.row
  article.col-lg-8
    header
      h1=document.title
      h4
        time(pubdate='')!=moment(document.date).format('MMM Do, YYYY')

    != content
