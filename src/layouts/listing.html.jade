---
layout: default
---

- var posts = getFiles({ relativeDirPath: 'blog' }, { date: -1 }).toJSON()

h1
  a(href='/blog')
    = document.title

ul.list-unstyled
  each post in posts
    if post.basename != 'index'
      li
        a(href=post.url)= post.title
        &nbsp; &bullet; &nbsp;
        =post.date
